<h1 class="text-4xl mt-36 mb-8 font-bold font-mono text-gray-800 text-center ">編輯商品</h1>
<div class="flex justify-end items-center w-11/12 md:w-8/12 lg:w-9/12 mx-auto">
  <% if Room.find_by(id: @product.id) == nil %>
    <%= link_to "前往建立房間", new_room_path( id: @product.id ),class: "p-2.5 text-center inline-block font-semibold text-gray-800 hover:bg-gray-200 whitespace-nowrap bg-blue-200 rounded-lg transition ease-in duration-200 shadow-2xl" %>
  <% else %>
    <%= link_to '前往編輯房間', edit_room_path(@product.id), class:"p-2.5 text-center inline-block font-semibold text-gray-800 hover:bg-gray-200 whitespace-nowrap bg-blue-200 rounded-lg transition ease-in duration-200 shadow-2xl" %>
  <% end %>
</div>
<%= render 'form', product: @product %>
