<% if @orders %>
<div class="antialiased bg-gray-100 text-gray-600 px-4">
  <div class="flex flex-col justify-center my-10">
    <div class="w-full max-w-2xl mx-auto bg-white shadow-lg rounded-sm border border-gray-200">
      <div class="p-3">
        <div class="">
          <table class="table w-full mx-auto">
            <!-- head -->
            <thead class="text-xs font-semibold text-white bg-blue-800">
              <tr>
                <th class="p-2 whitespace-nowrap text-lg">訂單編號</th>
                <th class="p-2 whitespace-nowrap text-lg">商品名稱</th>
                <th class="p-2 whitespace-nowrap text-lg">商品價格</th>
                <th class="p-2 whitespace-nowrap text-lg">訂單狀態</th>
                <th class="p-2 whitespace-nowrap text-lg">買家姓名</th>
              </tr>
            </thead>

            <tbody class=" text-sm divide-y divide-gray-100">
              <% @orders.each do |order| %>
                <tr class="my-10 hover:bg-blue-300 hover:shadow-2xl text-center p-10 transition ease-out duration-100">
                  <td class="p-2 whitespace-nowrap">
                    <div class="font-medium text-gray-800">
                      <%= order.id %>
                    </div>
                  </td>
                  <td class="p-2 whitespace-nowrap">
                    <div class="font-medium text-gray-800">
                     <%= order.description %>
                    </div>
                  </td>
                  <td class="p-2 whitespace-nowrap">
                    <div class="font-medium text-gray-800">
                      <%= order.price %>
                    </div>
                  </td>
                  <td class="p-2 whitespace-nowrap">
                    <div class="font-medium text-gray-800">
                      <%= order.status %>
                    </div>
                  </td>
                  <td class="p-2 whitespace-nowrap">
                    <div class="font-medium text-gray-800">
                      <%= User.find(order.buyer_id).username %>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<% else %>
<h1>尚未有結標商品</h1>
<% end %>
